{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Forester Extension",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}/editors/vscode",
                "${workspaceFolder}/examples"
            ]
        }
    ]
}
